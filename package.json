{
  "name": "project-twelve-contract-monorepo",
  "private": true,
  "version": "v0.0.3",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "compile": "pnpm recursive run compile",
    "lint": "pnpm recursive run lint",
    "test": "pnpm recursive run test"
  }
}